The project's conventions live in AGENTS.md, so that one file stays the source
of truth for every agent that reads this repo. The automated PR review looks for
this filename specifically, and checks changes against whatever it finds here.

@AGENTS.md
